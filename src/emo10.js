/** @jsxImportSource @emotion/core */
import { jsx } from '@emotion/core';

const E10 = () => (
  <div css={{ background: 'red', borderRadius: 3, padding: '0.5em', margin: 10 }}>
    Emotion 10 component
  </div>
);

export default E10;